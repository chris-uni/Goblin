using "io";
using "data";
using "strings";

let source = "using io;";
let chars = strings.split(source, "");
let count = data.size(chars);

for(let i = 0; i < count; i++;){

    let char = chars[i];

    if(char != "s"){
        io.println("It works!");
    }
    else {
        io.println(char);
    }
}